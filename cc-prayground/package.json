{
  "name": "@cc/prayground",
  "version": "0.1.0", 
  "description": "Experimental playground for prototypes and code trials",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo 'Starting playground development server'",
    "experiment": "echo 'Running experimental scripts'", 
    "build:prototype": "echo 'Building prototype'",
    "clean": "rm -rf temp/ sandbox/temp/ .cache/",
    "reset": "pnpm run clean && echo 'Playground reset complete'"
  },
  "keywords": [
    "playground",
    "experiment", 
    "prototype",
    "sandbox"
  ],
  "devDependencies": {},
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}